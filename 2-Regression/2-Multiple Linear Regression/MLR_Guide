editor
برای داده های کیفی از dummy varianle استفاده میکنیم
 ####dummy variable trap :
    استفاده از داده های بیش از اندازه دامی در مدل
     
    وقتی ۲ داده دامی در مدل استفاده می شود تنها باید یک متغیر در مدل استفاده شود
    
 